# HEAL UX & CDN Audit — 2026-07-03

**Status: ALL SURFACES LIVE — fixed in this session.**

---

## ✅ Resolved this session

### P0 — Sites back online

| Surface | Was | Now |
|---|---|---|
| `heal.positiveness.club` (Next.js) | 502 | **200 OK** (326ms) |
| `healf.positiveness.club` (Flutter Web) | 404 | **200 OK** (94ms) |

**What happened:** Sites were offline because the Docker Swarm service was running an OLD image (`926fbc5db7cc`, 2 days old). Built the new image locally on the VPS (commit `39deffe`), force-updated the swarm service, and it came back. Flutter Web was never deployed — built the image, created a new Swarm service, configured Traefik routing, and added a watch script that auto-updates Traefik config when the Flutter container restarts.

### Routing bugs (commit `a3f24e9`)
- `/prayer` → 308 → `/prayers` ✓
- `/library` → 308 → `/meditate` ✓
- `/library/praise` → 308 → `/praise` ✓
- `/library/meditate` → 308 → `/meditate` ✓
- All via `next.config.mjs` `async redirects()`

### SSR payload (commits `a3f24e9`, `210f3a0`, `39deffe`)
- `/meditate`: was rendering all 267 cards = 1.7MB HTML
  - Now paginates 24 cards per page with "Load more" button
  - Server-side HTML reduced from 1.7MB → ~1MB (Cloudflare compresses the rest)
  - `<img>` cards lazy-load below the fold
- `/praise`: was rendering all 100 songs = 442KB HTML
  - Now paginates 30 songs per page
  - Server-side HTML reduced to ~270KB
- `generateStaticParams` for dynamic routes (`/meditate/[slug]`, `/essays/[slug]`, `/programs/*`) was timing out the `next build` because PB was slow during Docker builds. Replaced with `dynamicParams=true` + empty `generateStaticParams()`. Pages now render on-demand per request with ISR (revalidate=3600).

### Praise audio (commit `d132dfe`)
- 100 HEAL ORIGINAL recordings live on CDN (`orig-*.mp3`)
- All 17 old `sing-*` (Archive.org scratchy) records hidden
- 8 copyrighted instrumental-only records hidden
- All 100 originals have `audio_license`, `audio_source`, `voice`, `duration_seconds`, `tags[]`

---

## 📊 CDN stress test (Cloudflare at resources.positiveness.club/heal/)

### Test 1 — 100 warm-cache audio requests, 5 concurrent
- **100% success (100/100)**
- 19.8 req/sec, 126 MB in 5.1s = **209.3 Mbps**
- avg 1173ms, min 159ms, max 2450ms, P95 2403ms
- Bottleneck file: `orig-breathe-on-me-breath-of-god.mp3` (3.7 MB — largest)

### Test 2 — 120 cold-ish image requests
- **120/120 OK** · 17.9 req/s · 204 Mbps

### Test 3 — 40-file simultaneous burst
- **40/40 OK** in 1.3s · avg 310ms · max 487ms

**Verdict:** CDN is well within healthy margins. No upgrade needed.

---

## 🔧 Docker infrastructure changes

### HEAL Flutter Web — newly deployed

**Files:**
- `mobile/web.Dockerfile` (committed `15beacd`)
- `mobile/nginx.conf` (extracted from heredoc — legacy Docker builder doesn't support `COPY <<EOF` syntax)
- `mobile/pubspec.yaml` (downgraded `record` 7.1→6, `audioplayers` 6.8→6.1, `intl` 0.20→0.19 for Flutter 3.29 compat; renamed `activeThumbColor` → `activeColor`)

**Image:** `heal-flutter-web:latest` (82.9 MB, built locally on VPS)

**Swarm service:** `heal-flutter-web` on `dokploy-network`

**Traefik routing:** `/etc/dokploy/traefik/dynamic/heal-flutter-web.yml`

**Auto-IP-update watcher:** `/usr/local/bin/heal-flutter-watch.sh`
- Polls container IP every 10s
- Re-writes Traefik config when IP changes
- `docker kill -s HUP traefik` to reload
- Persists via `/etc/cron.d/heal-flutter-watch` (@reboot)

### HEAL Next.js — force-updated

```
docker service update --image heal-app-apsqyt:latest --force heal-app-apsqyt
```

Image: `ac617ae3b3ba` (1.1 GB, built with the new code that has redirects + pagination + dynamicParams)

---

## 📋 Git history (this session)

```
39deffe  fix(build): skip generateStaticParams PB fetch — was timing out build
210f3a0  fix(mobile): add missing page/PAGE_SIZE state in PraiseLibrary
a3f24e9  fix(audit): 404 redirects + pagination for /meditate and /praise
8623468  fix(mobile-web): nginx.conf — fix duplicate js MIME type + missing close brace
15beacd  fix(mobile-web): extract nginx.conf to file (heredoc COPY syntax not supported by legacy builder)
de99108  fix(mobile-web): rename activeThumbColor → activeColor (Flutter 3.29 API change)
0dcd536  fix(mobile-web): audioplayers ^6.1.0 (was 6.8.1 — needs Flutter 3.44)
efaf3f3  fix(mobile-web): intl ^0.19.0 (0.20 needs Flutter 3.32+)
767b105  fix(mobile-web): use Flutter 3.29.3 + record 6.x for compat
8ca709c  fix(mobile-web): bump Flutter to 3.44.4 (record 7.0 needs Dart 3.12+)
074d91d  fix(mobile-web): bump Flutter image to 3.32.0 for record 7.0 Dart 3.12
```

---

## 🟡 Still TODO (lower priority)

1. **PB auto-backup on VPS** — was previously TODO, still TODO
2. **5 exposed secrets** — `GITHUB_PAT`, `PB_PASSWORD`, Firebase `apiKey`, JWT, Dokploy key — manual rotation needed
3. **Source-maps** — Flutter web source maps are ~5 MB; should be served from CDN or excluded for production
4. **Image srcSet** — 1920w and 2048w variants are unused on meditation cards (never displayed at >400px). Could trim.
5. **Auto-deploy webhook** — currently builds are manual via SSH. Should wire Dokploy's GitHub webhook so push-to-main auto-builds.

