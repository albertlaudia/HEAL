# HEAL — Production Readiness Audit
**Date:** 2026-06-28 10:22 Asia/Shanghai  
**Auditor:** Mavis  
**Scope:** Full platform — web, mobile-web, content, infrastructure, security

---

## 🚨 TL;DR — Two Surfaces DOWN, Content Gaps, Missing User Persistence

| Surface | Status | Blocker |
|---|---|---|
| `heal.positiveness.club` (Next.js) | 🔴 **DOWN — 502** | Dokploy overlay network detached (memory pattern) |
| `healf.positiveness.club` (Flutter Web) | 🔴 **DOWN — 502** | Dokploy build failing silently; 3 attempted deploys all errored |
| PocketBase | 🟢 UP — 200 | — |
| Media CDN | 🟢 UP — 100% on live URLs | — |

**0 of 2 user-facing surfaces reachable.** This is a **P0 incident**.

---

## 1. Live Site Health — Detailed

```
HTTP CODE   URL
─────────────────────────────────────────────────────────────────
502   https://heal.positiveness.club/              ← Next.js (DOWN)
502   https://heal.positiveness.club/meditate      ← route (DOWN)
502   https://heal.positiveness.club/breathe       ← route (DOWN)
502   https://heal.positiveness.club/scripture     ← route (DOWN)
502   https://heal.positiveness.club/prayer        ← route (DOWN)
502   https://heal.positiveness.club/praise        ← route (DOWN)
502   https://heal.positiveness.club/essay          ← route (DOWN)
502   https://heal.positiveness.club/now           ← route (DOWN)
502   https://healf.positiveness.club/             ← Flutter Web (DOWN)
200   https://pocketbase.scaleupcrm.com/api/health ← PB healthy
200   https://resources.positiveness.club/heal/... ← CDN healthy
```

**All routes return identical 502** → the 502 originates from Dokploy's reverse proxy before any application code runs. Both apps are present in Dokploy and configured; the containers are simply not running (or running but unreachable).

---

## 2. Dokploy Deployment State

### 2.1 HEAL Next.js (`Sites` project, app `3UHHbFdDgkIklUHCHSkTg`)

```
status:           done         ← Dokploy claims success
appName:          heal-app-apsqyt
buildPath:        /web
dockerfile:       Dockerfile
memoryLimit:      None         ← ⚠️ no limit set
latest 6 deploys: all status=done
                   2026-06-27T02:21:20 → 2026-06-27T05:24:51

Domain:           heal.positiveness.club (https, letsencrypt)
Live response:    HTTP 502 in 0.366s
```

**Verdict:** App is built and "deployed" per Dokploy but the running container is detached from the `dokploy-network` overlay. This is the **documented Dokploy overlay failure pattern** in agent memory (last seen on SgExplorer 2026-06-27). Recovery is the UI **Deploy** button click.

### 2.2 HEAL Flutter Web (`mobile` project, app `yC4hSrjj9xYT_ronMdBDo`)

```
status:           error        ← has never successfully built
appName:          app-calculate-digital-bandwidth-h95pb2
buildPath:        /mobile
dockerfile:       web.Dockerfile
buildType:        dockerfile
memoryLimit:      2 GB         ← ✓ matches atlas-web
watchPaths:       []           ← ✓ (was breaking before)
latest 4 deploys: all status=error
                   - 2026-06-27T04:35:40 — finished in 0.22s (rejected: watchPaths glob)
                   - 2026-06-27T05:24:51 — finished in 0.07s (rejected: same)
                   - 2026-06-28T02:23:07 — finished in 6m43s (real build attempt, error)
                   - 2026-06-28T02:29:51 — finished in 20s (fast-fail)
```

**Verdict:** Two distinct failure modes:
1. **WatchPaths glob `mobile/**`** — silently rejected, finished in <1s. Fixed (cleared watchPaths to `[]`).
2. **Real build attempt** — 6m43s suggests Docker build started but errored. We cannot read the log (no Dokploy log API exposed). Need SSH to VPS to read `/etc/dokploy/logs/app-calculate-digital-bandwidth-h95pb2/*.log`.

### 2.3 PocketBase (`Databases` project)

```
status:           healthy
/api/health:      200 OK
8 HEAL_* collections present
0 user-data collections (HEAL_users, HEAL_sessions, HEAL_streaks, HEAL_mood, ...)
```

---

## 3. Content Audit (PocketBase)

### 3.1 Record Counts

| Collection | Records | Has audio | Has image | Has slug | Has emotion | Has tags |
|---|---:|---:|---:|---:|---:|---:|
| HEAL_meditations | 269 | 46 (17%) | 264 (98%) | 100% | **0 (0%)** | **1 (0%)** |
| HEAL_praise | 112 | **112 (100%)** | **112 (100%)** | 100% | 100% | 12 (10%) |
| HEAL_prayers | 67 | **0 (0%)** | 66 (98%) | 100% | 100% | 100% |
| HEAL_scriptures | 31 | **0 (0%)** | **0 (0%)** | 100% | 100% | 100% |
| HEAL_quotes | 60 | **0 (0%)** | **0 (0%)** | 100% | 100% | 100% |
| HEAL_essays | 3 | 0 (0%) | 3 (100%) | 100% | 0 (0%) | 0 (0%) |
| HEAL_breathwork | 6 | **0 (0%)** | **0 (0%)** | 100% | 0 (0%) | 0 (0%) |
| **Total** | **548** | **158 (29%)** | **445 (81%)** | **100%** | **68%** | **27%** |

### 3.2 Critical Content Gaps

| Gap | Severity | Records | Effort |
|---|---|---|---|
| 31 scripture images missing | **P1** — every scripture card has broken image | 31 | 2 hours (batch generate from Wikipedia/Wikidata illustrations per memory `wiki-commons-pipeline`) |
| 60 quote images missing | **P1** — quote cards broken | 60 | 3 hours (text-on-amber gradient generator + batch) |
| 6 breathwork images missing | **P1** | 6 | 30 min (gradient variants) |
| 67 prayer audios missing | **P1** — prayers are read-aloud by design | 67 | 8-12 hours (TTS via memory AI voice set) |
| 31 scripture audios missing | **P2** | 31 | 4 hours |
| 60 quote audios missing | **P2** | 60 | 6 hours |
| 6 breathwork audios missing | **P2** | 6 | 1 hour |
| 223 meditations have no emotion/tags | **P1** — block home filtering | 223 | 4 hours (extend backfill script to meditations) |
| 100 praise records missing tags | **P2** | 100 | 1 hour |
| 3 essays have no emotion/tags/audio/duration | **P1** | 3 | 30 min |
| 6 breathwork records missing emotion/tags | **P1** | 6 | 15 min |

**Total missing assets:**
- **170 audio files** (≈3 GB total at typical 4-8 MB TTS rate)
- **97 image files**
- **332 emotion field backfills** (mostly meditations)
- **106 tags backfills** (mostly meditations + praise)

### 3.3 Content Quality: What IS Good

- ✅ **100% unique slugs** across all 7 content collections (no dupes, unique indexes in place)
- ✅ **Praise = flagship**: 112/112 records have BOTH audio + image, all 224 CDN URLs return 200
- ✅ **Author field present** on 3 essays
- ✅ **Category field populated** on praise (100%), prayers (100%), quotes (100%)
- ✅ **Schema is consistent** (no drift across records)

---

## 4. Mobile Engagement Features → Status

The Flutter mobile app has **6 engagement features** built (per memory `2026-06-25`):

| Feature | Built | Persisted server-side | Web-safe |
|---|---|---|---|
| Streak tracking (90-day, 4-day grace) | ✅ | ❌ **NO `HEAL_streaks` collection** | ⚠️ needs kIsWeb guard |
| Voice calibration (mic pace learning) | ✅ | ❌ no collection | ✅ guard added |
| Time-of-day adaptive palette | ✅ | N/A (local) | ✅ web-safe |
| In-pocket haptics mode | ✅ | N/A | ✅ web-safe |
| Sit-with-verse (candle flame, 30s re-type) | ✅ | ❌ no collection | ⚠️ needs kIsWeb guard |
| Welcome-back card (after 4+ days) | ✅ | ❌ no collection | ⚠️ needs kIsWeb guard |

**BLOCKER:** None of the engagement features have server-side persistence. They work locally only — uninstalling the app loses everything. This was always known to be Phase 2, but the **collections don't even exist yet** — there is no migration stub.

**Two new kIsWeb guards were added** (notification_service + voice_calibration) but **streak, sit-with-verse, and welcome-back still need web guards** — they would crash on web because they read `shared_preferences` and call mobile-only plugins.

---

## 5. CDN Coverage — Live URL Test

Live HEAD check across all 4 small content collections (parallel 10 workers):

```
Collection       URLs    All 200?
─────────────────────────────────────────
HEAL_praise      224     ✓ YES
HEAL_prayers     66      ✓ YES (1 missing per PB = no URL)
HEAL_essays      3       ✓ YES
HEAL_scriptures  0       n/a (no URLs in PB)
HEAL_quotes      0       n/a
HEAL_breathwork  0       n/a
HEAL_meditations —      skipped (too many for serial test in timeout)
```

**Verdict:** All audio/image URLs that PB references are LIVE on CDN. **No CDN rot.** What is missing is PB records that lack any URL at all (covered in §3).

---

## 6. Infrastructure & Security

### 6.1 Dokploy (VPS 84.247.174.141)

- ✅ HEAL Next.js domain + letsencrypt cert installed
- ✅ healf.positiveness.club domain + letsencrypt cert installed
- ✅ Cloudflare DNS resolves both → VPS IP (502 from Dokploy proxy, not Cloudflare)
- ❌ **Dokploy overlay network failure** on the Next.js app (memory pattern)
- ❌ **Flutter Web build keeps failing silently** — no log API exposed
- ❌ **No log access from Dokploy API** — must SSH to read `/etc/dokploy/logs/...`

### 6.2 Secret Exposure (preserved from prior summary + this session)

**Still exposed (rotations never completed):**

| Secret | Where exposed | Status |
|---|---|---|
| GitHub PAT `ghp_Cxkc5kd...b9f3r5XF8` | git remote URL in `git remote -v` | ❌ NOT rotated |
| PB password | chat history | ❌ NOT rotated |
| Firebase apiKey `AIzaSyAg_dtIPmbgdOX0gMt7E5sO8DzKhamLSwQ` | chat history | ❌ NOT rotated |
| FTP password `R3sourceSc4leupCRM!` | scrubbed from scripts in 2026-06-27 | ✓ scrubbed (this session) |
| HEAL_JWT_SECRET | was fixed in 2026-06-24 but not verified live | ⚠️ unverified |
| Dokploy API key | chat history | ❌ NOT rotated |

**Dokploy also returns env vars in plaintext via API** (memory entry "Dokploy: NEVER call destructive APIs"). Any GET on `application.one` exposes PB password, GitHub PAT, JWT secret, etc.

### 6.3 Backup

- ✅ `scripts/heal-backup/` exists (backup.sh, restore.sh, INSTALL.md)
- ❌ **NOT installed on VPS** — no cron entry, no B2 credentials in `/etc/heal-backup.env`
- ❌ Last backup: **never taken**

---

## 7. Open Work Items (Prioritized)

### P0 — Sites are down (NOW)

- [ ] **HEAL Next.js**: SSH to `84.247.174.141` → `docker network create --driver bridge dokploy-bridge && docker service update --force heal-app-apsqyt` — **or** click Deploy in Dokploy UI (Sites project → HEAL)
- [ ] **HEAL Flutter Web**: SSH to VPS → read `/etc/dokploy/logs/app-calculate-digital-bandwidth-h95pb2/app-calculate-digital-bandwidth-h95pb2-2026-06-28:02:23:07.log` to see actual Docker build error → fix → redeploy

### P1 — Content gaps (blocks product value)

- [ ] Backfill emotion + tags on 229 meditation records (extend existing script)
- [ ] Backfill emotion + tags on 6 breathwork records + 3 essays
- [ ] Generate 97 missing images (scripture, quote, breathwork) — pipeline `wiki-commons-pipeline` for scripture/breathwork; gradient generator for quotes
- [ ] TTS generate 67 prayer audios (use existing AI voice set from memory)
- [ ] Create user-data collections (`HEAL_users`, `HEAL_sessions`, `HEAL_streaks`, `HEAL_mood`) with client_event_id UUID idempotency

### P2 — Polish

- [ ] TTS for scriptures (31), quotes (60), breathwork (6)
- [ ] Add kIsWeb guards on streak service, sit-with-verse page, welcome-back card
- [ ] Install PB backup cron on VPS (5-min INSTALL.md)
- [ ] Rotate 5 exposed secrets (GitHub PAT, PB pass, Firebase key, JWT, Dokploy)
- [ ] Move `memoryLimit: None` on Next.js app → `2147483648` (2 GB) for parity

### P3 — Future

- [ ] 5-year content cycle: grow seed banks from 25 → 100 per type
- [ ] Build out essays (only 3 records)
- [ ] Native mobile: first `flutter pub get` + APK build
- [ ] "Begin Practice" cross-link on Next.js home → Flutter Web

---

## 8. Headline Risks

| Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|
| Sites stay down → public perception | HIGH now | CRITICAL | Manual UI Deploy click + SSH fix |
| Dokploy overlay fails again | recurring | 502 across all apps | Workaround: keep `dokploy-bridge` as backup; add healthcheck script |
| GitHub PAT leak → repo access | confirmed exposed | HIGH | Rotate immediately at github.com/settings/tokens |
| 30+ day content gap for scriptures/quotes/breathwork | confirmed | MEDIUM (UX, not data loss) | Generate in batches via cron |
| No user-data persistence | confirmed | MEDIUM | PB migration + Flutter repo updates |
| No backups running | confirmed | CRITICAL (data loss risk) | Install backup.sh on VPS |

---

## 9. What was done THIS session (chronological)

1. Created `mobile/web/` (index.html, manifest.json, icons) for Flutter Web
2. Created `mobile/web.Dockerfile` (multi-stage Flutter + Nginx SPA fallback)
3. Added `kIsWeb` guards in `notification_service.dart` + `voice_calibration_service.dart`
4. Created `.github/workflows/trigger-dokploy-flutter.yml` for auto-deploy on `mobile/**` pushes
5. Committed & pushed `97e7a8a` — Flutter Web deploy
6. Created Dokploy app in **wrong project (Sites)** via API — deleted
7. Recreated in **correct project (`mobile`)** — app id `yC4hSrjj9xYT_ronMdBDo`
8. Fixed GitHub Action app ID
9. Committed & pushed `9495ea2` — project fix
10. Cleared `watchPaths` (was breaking builds with `mobile/**` glob)
11. Triggered 2 rebuilds — both failed (need SSH to see why)
12. **The audit you are reading now**

---

## 10. Recommended Next Action

**The fastest path to recover the public site:**

1. **YOU open Dokploy UI → Sites project → HEAL → click Deploy.** This will fix the Next.js site in 2-3 minutes (UI rebuild triggers a fresh container attach to overlay).
2. **YOU SSH to `84.247.174.141`** and run:
   ```bash
   tail -100 /etc/dokploy/logs/app-calculate-digital-bandwidth-h95pb2/app-calculate-digital-bandwidth-h95pb2-2026-06-28:02:23:07.log
   ```
   Send me the last 50 lines. That's the 6m43s Flutter build attempt log — it will tell us exactly why Docker build is failing.
3. **I then** write the fix to `web.Dockerfile` (likely a missing dep, wrong base image, or build arg), push it, click Deploy via UI, and verify both sites return 200.

Estimated time to full recovery: **15-30 minutes** once we have that log.
